<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Save -->
    <item
        android:id="@+id/save"
        android:title="@string/save"
        android:icon="@drawable/save"
        android:orderInCategory="10"
        app:showAsAction="ifRoom" />

    <!-- Run & Build APK submenu -->
    <item
        android:icon="@drawable/run"
        android:orderInCategory="20"
        app:showAsAction="always"
        app:iconTint="@color/run_icon">
        <menu>
            <item
                android:id="@+id/run"
                android:title="@string/run"
                android:icon="@drawable/run"
                android:orderInCategory="1"
                app:showAsAction="always"
                app:iconTint="@color/run_icon" />
            <item
                android:id="@+id/build_apk"
                android:title="@string/build_apk"
                android:icon="@drawable/run"
                android:orderInCategory="2"
                app:showAsAction="always"
                app:iconTint="@color/run_icon" />
        </menu>
    </item>

    <!-- Format -->
    <item
        android:id="@+id/format"
        android:title="@string/format"
        android:icon="@drawable/format_text"
        android:orderInCategory="30"
        app:showAsAction="ifRoom" />

    <!-- Color Picker -->
    <item
        android:id="@+id/color_picker"
        android:title="@string/color_picker"
        android:icon="@drawable/palette"
        android:orderInCategory="40"
        app:showAsAction="ifRoom" />

    <!-- Encrypt Tools -->
    <item
        android:id="@+id/encode_tools"
        android:title="Encrypt Tools"
        android:icon="@drawable/encode"
        android:orderInCategory="50"
        app:showAsAction="ifRoom" />

    <!-- Markdown Preview -->
    <item
        android:id="@+id/markdown_preview"
        android:title="@string/markdown_preview"
        android:icon="@drawable/markdown"
        android:orderInCategory="60"
        app:showAsAction="always"
        app:iconTint="@color/markdown_preview_icon" />

    <!-- Git submenu -->
    <item
        android:id="@+id/git"
        android:title="@string/git"
        android:icon="@drawable/git"
        android:orderInCategory="70"
        app:showAsAction="always"
        app:iconTint="@color/git_icon">

        <menu>
            <item
                android:id="@+id/git_add"
                android:title="@string/git_add"
                android:icon="@drawable/plus"
                android:orderInCategory="10" />

            <item
                android:id="@+id/git_commit"
                android:title="@string/git_commit"
                android:icon="@drawable/save"
                android:orderInCategory="20" />

            <item
                android:id="@+id/git_push"
                android:title="@string/git_push"
                android:icon="@drawable/send"
                android:orderInCategory="30" />

            <item
                android:id="@+id/git_pull"
                android:title="@string/git_pull"
                android:icon="@drawable/download"
                android:orderInCategory="40" />

            <item
                android:id="@+id/git_status"
                android:title="@string/git_status"
                android:icon="@drawable/status"
                android:orderInCategory="50" />

            <item
                android:id="@+id/git_init"
                android:title="@string/git_init"
                android:icon="@drawable/folder_plus"
                android:orderInCategory="60" />

            <item
                android:id="@+id/git_remote"
                android:title="@string/git_remote"
                android:icon="@drawable/cloud_config"
                android:orderInCategory="70" />

            <item
                android:id="@+id/git_auth"
                android:title="@string/git_auth"
                android:icon="@drawable/key"
                android:orderInCategory="80" />
        </menu>
    </item>

    <!-- Settings -->
    <item
        android:id="@+id/settings"
        android:title="@string/settings"
        android:icon="@drawable/settings"
        android:orderInCategory="80"
        app:showAsAction="ifRoom" />

</menu>
